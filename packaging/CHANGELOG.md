# Changelog

## 1.0.0

- Adds deterministic macOS distribution layout with `.ofx.bundle`, sidecar resources, root release metadata, model manifest copies, offline model manifest example, uninstaller artifact, and distribution manifest.
- Adds opt-in purge uninstaller behavior; user model/log directories are preserved by default.
- Adds release packaging notes for macOS, dependencies, known issues, and install/uninstall evidence capture.
